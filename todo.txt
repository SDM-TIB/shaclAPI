- Parse Initial Star Shaped Query correctly into CONSTRUCT Query e.g. ORDER BY, LIMIT, OFFSET Terms
- Autoretrieve Namespaces
- more then one filter --> slow down
- PrefixHandling: --> Example Prefixes extracted from the star shaped query should be added to the SPARQLPrefixHandler

- Causes Simplification Error (1 Filter without != =)

SELECT DISTINCT ?x WHERE {
?x test2:refersTo ?inst.
?x a test2:classA.
FILTER (?inst NOT IN ( <http://example.org/testGraph2#nodeB_1> )). }


- Allow all kind of FILTER's in star shaped queries.