API can be tested with 
- curl -X POST -H "Accept:application/sparql-results+json" -d "config=./examples/vaccinated_people/config.json" http://127.0.0.1:9999/multiprocessing
which will start the validation with the given config.json with the shapes provided in ./shapes/